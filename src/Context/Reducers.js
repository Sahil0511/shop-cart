import React from "react";

export const cartReducer = (state, action) => {
  switch (action) {
    default:
      return state;
  }
};

const Reducers = () => {
  return <></>;
};

export default Reducers;
